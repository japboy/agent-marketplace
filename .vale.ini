StylesPath = styles
MinAlertLevel = error

[*.{md,mdown,markdown,markdn}]
BasedOnStyles = AgentMarketplace
# Keep HTTP(S) autolinks out of prose checks while retaining ordinary link text.
# Vale 3.20 includes autolink labels in its text scope.
TokenIgnores = (<https?://[^<>\s]+>)
